apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openshift-gitops

commonLabels:
  created-by: cluster-bootstrap
  author: "OpenShift Cluster Bootstrap"

resources:
- cluster-config-project.yaml
