apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - install-quay.yaml

patches:
  - patch: |-
      - op: replace 
        path: /spec/source/path
        value: clusters/management-cluster/config/redhat-quay/overlays
    target:
      name: "install-quay-operator"
      kind: Application
      group: argoproj.io
      version: v1alpha1