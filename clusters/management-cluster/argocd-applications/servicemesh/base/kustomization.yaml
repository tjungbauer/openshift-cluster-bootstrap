apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - install-servicemesh.yaml

patches:
  - patch: |-
      - op: replace 
        path: /spec/source/path
        value: clusters/management-cluster/config/servicemesh/overlays
    target:
      name: "install-servicemesh-operator"
      kind: Application
      group: argoproj.io
      version: v1alpha1