apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - install-sso.yaml

patches:
  - patch: |-
      - op: replace 
        path: /spec/source/path
        value: clusters/management-cluster/config/redhat-sso/overlays
    target:
      name: "install-sso-operator"
      kind: Application
      group: argoproj.io
      version: v1alpha1