apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - install-ocs.yaml

patches:
  - patch: |-
      - op: replace 
        path: /spec/source/path
        value: clusters/management-cluster/config/openshift-container-storage/overlays
    target:
      name: "install-ocs-operator"
      kind: Application
      group: argoproj.io
      version: v1alpha1