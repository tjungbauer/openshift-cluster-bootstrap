apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - install-resource-locker.yaml

patches:
  - patch: |-
      - op: replace 
        path: /spec/source/path
        value: clusters/management-cluster/config/resource-locker/overlays
    target:
      name: "install-resource-locker-operator"
      kind: Application
      group: argoproj.io
      version: v1alpha1